version: "3"
services:
  brain:
    build: brain/
    ports:
      - 1234:1234
  monitor:
    build: monitor/
    depends_on:
      - brain
    ports:
      - 80:80
  robot:
    build: robot/
    network_mode: host
    depends_on:
      - brain
